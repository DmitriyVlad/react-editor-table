import './button-close.scss';
import React from 'react';

const ButtonClose = () => {

  const classBase = 'ButtonClose';

  return (
    <button className={ classBase } />
  );
};

export default ButtonClose;
